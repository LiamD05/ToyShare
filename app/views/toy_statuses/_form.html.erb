<%= form_with(model: toy_status) do |form| %>
  <% if toy_status.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(toy_status.errors.count, "error") %> prohibited this toy_status from being saved:</h2>

      <ul>
        <% toy_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
